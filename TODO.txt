middlewares 
====

/**
 * paranos : ocmise all ocm protocol in deep.Promise.context.protocols
 * @return {[type]} [description]
 */
autobahn.paranos = function(){
	// args : list of groups to ocmise in deep.protocols (check any OCM protocol)
}


use node-range-parser to parse range header
